const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function renderSaved(state){const items=state.savedItems||[];return`<section class="view"><div class="view-head"><p class="eyebrow">Saved library</p><h1>Your reusable intelligence.</h1><p class="lead">Search, filter, pin, and export research, scripts, calendars, and Instagram insights.</p><a class="button secondary" href="/api/export/all" download>Export all as ZIP</a></div><div class="library-controls"><input id="savedSearch" type="search" placeholder="Search saved items…" value="${esc(state.savedQuery||'')}"><div id="savedFilters" class="chips">${['all','research','instagram','scripts','calendar'].map(type=>`<button class="chip ${(state.savedFilter||'all')===type?'active':''}" data-filter="${type}">${type}</button>`).join('')}</div></div><div class="saved-list">${items.length?items.map(item=>`<article><div class="saved-icon">${item.source_type==='instagram'||item.type==='instagram'?'◎':'◇'}</div><div><span class="badge">${esc(item.type||'saved')}</span><h3>${esc(item.title||'Saved insight')}</h3><p>${esc((item.content||'').replace(/[#*_>`]/g,'').slice(0,220))}</p></div><div class="saved-actions"><button data-open-saved="${esc(item.id)}">Open</button><button data-pin-saved="${esc(item.id)}">${item.pinned?'Unpin':'Pin'}</button><button class="danger" data-delete-saved="${esc(item.id)}">Delete</button></div></article>`).join(''):'<div class="empty"><strong>No matching items</strong><p>Save a research result or Instagram report to build your library.</p></div>'}</div></section>`;}
