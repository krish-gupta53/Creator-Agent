export const WORKSPACE_SCRIPT_F = String.raw`
function createProject(prompt){if(state.project)return Promise.resolve(state.project);return requestJson('/api/workspace/projects',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:prompt.slice(0,90),kind:state.kind})}).then(function(data){state.project=data.project;return data.project})}
function uploadSources(projectId){if(!state.files.length&&!state.urls.length)return Promise.resolve();var form=new FormData();state.files.forEach(function(file){form.append('files',file)});state.urls.forEach(function(url){form.append('urls',url)});return requestJson('/api/workspace/projects/'+encodeURIComponent(projectId)+'/sources',{method:'POST',body:form}).then(function(){state.files=[];state.urls=[]})}
function startRun(prompt){if(!String(prompt||'').trim())return showToast('Describe what to research or analyse.');createProject(prompt).then(function(project){return uploadSources(project.id).then(function(){return requestJson('/api/workspace/projects/'+encodeURIComponent(project.id)+'/runs',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({kind:state.kind,mode:state.mode,prompt:prompt,title:prompt.slice(0,110)})})})}).then(function(data){return openProject(state.project.id,data.run.id)}).then(function(){showToast('Research started.')}).catch(function(error){showToast(error.message)})}
function openProject(projectId,runId){return requestJson('/api/workspace/projects/'+encodeURIComponent(projectId)).then(function(data){state.project=data.project;state.project.runs=data.runs;state.project.attachments=data.attachments;state.run=runId?data.runs.find(function(item){return item.id===runId}):data.runs[0]||null;state.view='project';renderProjectList();renderCurrent();pollRun()})}
function renderProject(){if(state.run&&state.run.status==='completed'&&state.run.result)return renderReport();clear(root);document.getElementById('rTitle').textContent=state.project.title;var head=el('div','rhead');var title=el('div');title.append(el('h1','',state.project.title));title.append(el('div','muted',(state.project.attachments||[]).length+' sources · '+(state.project.runs||[]).length+' runs'));head.append(title);root.append(head);var grid=el('div','rgrid');var left=el('div');var reports=el('div','panel');reports.append(el('div','ptitle','Reports'));(state.project.runs||[]).forEach(function(run){var row=el('div','run');var name=el('strong','',run.title);row.append(name);row.append(el('small','',run.kind+' · '+run.mode.replace(/_/g,' ')+' · '+run.status));row.onclick=function(){state.run=run;renderCurrent();pollRun()};reports.append(row)});if(!(state.project.runs||[]).length)reports.append(el('div','empty','No reports yet.'));left.append(reports);left.append(el('div','',''));var prompt=el('textarea','rprompt');prompt.placeholder='Ask another research or content-analysis question in this project.';var composer=el('div','rcompose');composer.append(prompt);var actions=el('div','rcomposefoot');actions.append(makeButton('Add sources',openSourceDialog));actions.append(el('span','grow'));actions.append(makeButton('Run',function(){startRun(prompt.value)},true));composer.append(actions);left.append(composer);grid.append(left);var sources=el('aside','panel');sources.append(el('div','ptitle','Project sources'));(state.project.attachments||[]).forEach(function(source){var row=el('div','source');row.append(el('strong','',source.name));row.append(el('small','',source.status+' · '+(source.extraction_method||'source')));sources.append(row)});if(!(state.project.attachments||[]).length)sources.append(el('div','empty','Add URLs, documents, audio, images, or video.'));grid.append(sources);root.append(grid)}
`;
