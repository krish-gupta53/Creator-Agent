{
  "name": "personalized-content-agent",
  "version": "4.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node scripts/check.mjs && wrangler deploy --dry-run --outdir .wrangler-build",
    "db:local": "wrangler d1 migrations apply DB --local",
    "db:remote": "wrangler d1 migrations apply DB --remote",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "unpdf": "^1.6.2"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
