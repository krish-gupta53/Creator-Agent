{
  "name": "creatoriq",
  "version": "6.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js && node --check src/router.js && node --check src/frontend/app.js && wrangler deploy --dry-run --outdir .wrangler-build",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
